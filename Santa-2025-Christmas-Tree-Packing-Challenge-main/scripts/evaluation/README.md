# scripts/evaluation

Avaliação local do `submission.csv`.

Recomendado:

* `python -m santa_packing.cli.score_submission submission.csv --pretty`
* ou via Python: `from santa_packing.scoring import score_submission`

O scorer “de verdade” vive em `santa_packing/scoring.py`.
